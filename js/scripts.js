var section, vcontent, isLogin, base_url;

$(window).ready(function() {
	$lgdat = $("#lgdat");
	base_url = $lgdat.attr("base_url");
	section = $lgdat.attr("section");
	vcontent = $lgdat.attr("vcontent");
	isLogin = $lgdat.attr("isLogin");
	
	//console.log(base_url+" - "+section+" - "+vcontent+" - "+isLogin);
	
});